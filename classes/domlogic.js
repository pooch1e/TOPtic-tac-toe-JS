//need to handle clicks and then send data to the board
//need a way to handle the arrays in gameboard

class DomLogic {
  constructor(button, id) {
    this.x = null;
    this.y = null;
    this.button = button;
    this.id = id;
  }

  renderObject(gameScreen) {
    //render array to game screen

    

    
  }
}