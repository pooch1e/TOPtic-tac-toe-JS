//need to handle clicks and then send data to the board

class Dom {
  constructor(button) {
    this.x = null;
    this.y = null;
    this.button = button;
  }

  renderObject(GameboardArray) {
    //renders object to the screen
    
  }
}